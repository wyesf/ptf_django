// const simpleParallax = require('simple-parallax-js');

// parallax
var prximg = document.getElementsByClassName('thumbnail');
new simpleParallax(prximg, {
    orientation: 'right',
    scale: 0.7,
});
// instance.refresh();
